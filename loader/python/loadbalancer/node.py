class Node(object):
	"""docstring for Node"""
	def __init__(self, address, port, path):
		super(Node, self).__init__()
		self.arg = arg
		self.ip=address
		self.access_port=port #set the access port for the node
		self.base_path = path #set the path on where to execute the script
	def access(self): #access the base path of a node
		pass
		